import * as React from 'react'
import { render } from '@testing-library/react'

describe('render component <>DateSelectorComponent />', () => {
  it('should render DateSelectorComponent and select year', () => {
    const {screen} = render(<DateSelector />)
    expect()
  })
})
